
/*
import React, { useState } from 'react'
import axios from 'axios'

const cartButtonClick = async (id) => {
    /*
    const [item, setItem] = useState({
        c_id: null,
        c_name: "",
        c_price: null,
        c_img: "",
        c_user: 987,
    })
    try {
        const get_prd = await axios.get("http://localhost:8800/selected/"+id)
        console.log(get_prd.data[0].id);        
    } catch (err) {
        console.log(err)
    }
}

export default cartButtonClick

*/